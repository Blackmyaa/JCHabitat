<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow">
	<div
		class="container-fluid">
		{# Bouton toggle pour mobile #}
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNavbar" aria-controls="adminNavbar" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		{# Menu principal #}
		<div class="collapse navbar-collapse" id="adminNavbar">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item text-light">
					<img src="{{asset('images/Logo-JCHabitat.JPG')}}" alt="Logo" width="50" height="40" class="d-inline-block align-text-top" data-bs-toggle="modal" data-bs-target="#imageModal">
				</li>

				<li class="nav-item">
					<a class="nav-link" href="{{ path('admin_dashboard') }}">Dashboard</a>
				</li>
                <li class="nav-item">
					<a class="nav-link position-relative" href=" {{ path('app_demandes_devis') }}">
						Demande de Devis
					</a>
				</li>
                <li class="nav-item">
					<a class="nav-link position-relative" href="{#  {{ path('admin_devis_index') }} #}">
						Nouveaux Devis
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link position-relative" href=" {{ path('admin_devis_index') }}">
						Liste des Devis
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('admin_client_index') }}">Clients</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{# {{ path('admin_products_list') }} #}">Prestations</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('admin_administrateur_index') }}">Gestion Admin</a>
				</li>

			</ul>

			{# Section utilisateur et déconnexion #}
			<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
				{% if app.user %}
					<li class="nav-item">
						<span class="navbar-text me-3">
							Bonjour,
							{{ app.user.prenom }}
						</span>
					</li>
				{% endif %}
				<li class="nav-item">
					<a class="nav-link text-danger" href="{{ path('app_logout') }}">Déconnexion</a>
				</li>
			</ul>
		</div>
	</div>
</nav>
